export interface TopSounds {
    artist: object;
    duration: string;
    image: any;
    name: string;
    listeners: string;
    mbid: string;
    playcount: string;
    url: string;
    streamable: object;
}
